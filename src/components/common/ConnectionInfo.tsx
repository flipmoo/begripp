import React from 'react';

// Completely empty component - no status bar on dashboard
const ConnectionInfo: React.FC = () => {
  // Return null to not render anything
  return null;
};

export default ConnectionInfo; 